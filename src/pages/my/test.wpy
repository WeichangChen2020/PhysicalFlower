<style lang="less">
</style>
<template>
 <view @tap='test2({{a}}{{test}})' style="width:200px;height:20px;backgroud-color:#eee;">{{test}}</view>
</template>

<script>
import wepy from 'wepy'

export default class test extends wepy.page {
  data = {
    test: 1,
    a: 1,
    b: 2
  }

  // computed = {

  // }

  methods = {
    test2(e) {
      this.test = this.test + 1
      console.log(e)
    }
  }

  // events = {

  // }

  onShow() {
    var promise = new Promise(function(resolve) {
      resolve(42)
    })
    promise.then(function(value) {
      console.log(value)
    }).catch(function(error) {
      console.error(error)
    })
  }

  f1(resolve, reject) {
    console.log('f1')
    // let num = Math.random() * 2
    // setTimeout(function() {
    //   if (num < 1) {
    //     console.log('<1,resolve')
    //     resolve('11')
    //   } else {
    //     console.log('>1,reject')
    //     reject('22')
    //   }
    // }, num * 1000)
  }
}
</script>
